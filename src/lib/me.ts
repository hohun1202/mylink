// "내 페이지"의 핸들입니다.
// 로그인·핸들 설정(F3)이 아직 없어서 Mock 사용자 한 명을 페이지 주인으로 정해 둡니다.
// 3단계에서 핸들 설정이 생기면 이 값 대신 로컬 사용자의 핸들을 씁니다.
// (스토어 파일과 따로 둔 이유: 서버 컴포넌트인 app/page.tsx 도 이 값을 가져다 쓰는데,
//  스토어 파일에는 브라우저 전용 코드(useEffect)가 있어 서버 컴포넌트에서 불러올 수 없기 때문)
export const MY_HANDLE = "hohun1202";
